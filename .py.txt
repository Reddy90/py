class average:
def__init__(self,f,s)
self.fisrtnumber=f
self.secondnumber=s
    def display(self):
      print("first number="str(self.first))
      print("second number="str(self.second))
      print("average of two numbers="str(self.answer))
       def calculate(self):
        self.answer=(self.first+self.second/2)
        x=int(input("enter a number:"))
        y=int(input("enter another number"))
        obj=average(x,y)
        obj=calculate()
        obj=display()